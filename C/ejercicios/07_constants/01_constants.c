/*
 * Ejercicio: Constantes (#define vs const)
 *
 * 1. Define una constante PI con #define que valga 3.1416
 *    Hazlo ANTES del main.
 *
 * 2. Dentro del main, declara una constante const double GRAVEDAD = 9.81;
 *
 * 3. Declara un radio (double) = 5.0 y calcula:
 *    - area = PI * radio * radio
 *    - perimetro = 2 * PI * radio
 *    Muestra ambos con 2 decimales.
 *
 * 4. Intenta cambiar el valor de GRAVEDAD (ej: GRAVEDAD = 9.8;)
 *    Compila el programa. ¿Qué error sale? Anota el error
 *    como comentario después de la línea que falla.
 *
 * 5. Define otra constante IVA con #define = 16
 *    Declara precio (float) = 200.0f
 *    Calcula precioFinal = precio + (precio * IVA / 100.0f)
 *    Imprime precio, IVA% y precioFinal.
 */
#include <stdio.h>

// --- define PI aquí ---



int main() {
    const double GRAVEDAD = 9.81;

    // --- escribe aquí ---


    return 0;
}
