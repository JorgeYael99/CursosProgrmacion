#include <stdio.h>

int main(){
    // inicializamos variables
    int a = 1;
    int b = 2;
    // Suma de variables a + b
    int suma = a+b;
    // Se mandad a imprimir variables y calculo
    printf("Suma de variables a + b: %d\n",suma);
    printf("peso de variable suma: %d bytes\n",suma);
    // Cadena de texto
    char string[]="Hola México";
    // Se manda a imrpimir la cadena de texto
    printf("%s\n",string);

    return 0;
}